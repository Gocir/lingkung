import 'package:flutter/material.dart';

class HowToRegister extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      title: Text('Cara membuat akun Linkung'),
      ),
      body: Center(
        child:  Text('Hello Cuk'),
      ),
    );
  }
}