import 'package:flutter/material.dart';

class HowToLogin extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      title: Text('Cara masuk ke akun Linkung'),
      ),
      body: Center(
        child:  Text('Hello Cuk'),
      ),
    );
  }
}